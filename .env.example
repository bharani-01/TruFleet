# TruFleet — Environment Variables Template
# Copy this file to .env and fill in your values

# Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase publishable/anon key (safe for server use)
# For full admin access (bypassing RLS), use your service_role key instead
SUPABASE_KEY=your_supabase_key_here

# Express server port
PORT=3000
NODE_ENV=development
